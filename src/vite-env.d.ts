// Injected by vite.config.ts's `define` - the epoch ms timestamp of when this bundle was built
// (or, in `npm run dev`, when the dev server started) - a rough proxy for "what version is this."
declare const __BUILD_TIME__: number;
